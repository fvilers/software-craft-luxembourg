// The main function is always the first code that runs.
fn main() {
    // `println!()` is a macro that writes a string to standard output. The macro is useful as it replaces code that
    // locks the stdout handle and then writes to it.
    println!("Hello, world!");
}
